let data = prompt("Enter Your Name");
      alert(`Good Morning, ${data}`);

      // <!-- ## Basic Operation, Mathematical Operation  -->
      let amount = 2080;
      let value = amount - 80;
      let plusVal = amount + 200;
      let mulVal = amount * 4;
      let divVal = amount/21;

      // <!-- Logical Operation -->